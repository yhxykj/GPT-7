
import Foundation

import UIKit
import ZKProgressHUD
import SVProgressHUD

class RMainController: UIViewController {
private var bundleLoginOpenDictionary: [String: Any]!
private var playing_max: Double = 0.0
private var canFixed: Bool = false



    
    @IBOutlet weak var searchTF: UITextField!
    @IBOutlet weak var collectionView: UICollectionView!
    @IBOutlet weak var bankView: UIView!
    
    var isCreate = false
    var classItems: [RVZMine] = []

@discardableResult
 func maximumPositionDisabled(isbdingFailed: Bool, configPrompt: [Any]!, detailslabelYloading: [String: Any]!) -> String! {
    var imagesd: Bool = false
    var json5: String! = String(cString: [119,101,98,109,105,100,115,0], encoding: .utf8)!
   for _ in 0 ..< 1 {
      imagesd = (72 <= (json5.count ^ (imagesd ? json5.count : 72)))
   }
   repeat {
      json5 = "\(json5.count % 1)"
      if json5 == (String(cString:[103,97,103,54,95,55,97,55,99,51,0], encoding: .utf8)!) {
         break
      }
   } while (json5 == (String(cString:[103,97,103,54,95,55,97,55,99,51,0], encoding: .utf8)!)) && (!imagesd && json5.count == 4)
   while (5 > json5.count && imagesd) {
       var detaillabelE: Bool = false
       var indicesy: String! = String(cString: [98,97,99,107,111,102,102,0], encoding: .utf8)!
       var weak_e6C: Int = 3
       var aboutW: String! = String(cString: [100,111,119,110,119,97,114,100,0], encoding: .utf8)!
      while (indicesy.hasSuffix("\(detaillabelE)")) {
         detaillabelE = aboutW.count >= 50
         break
      }
         detaillabelE = !detaillabelE || weak_e6C <= 7
          var q_viewL: String! = String(cString: [102,102,112,114,111,98,101,0], encoding: .utf8)!
          var failedO: Float = 1.0
         weak_e6C |= ((detaillabelE ? 2 : 2) % (Swift.max(aboutW.count, 9)))
         q_viewL.append("\(q_viewL.count >> (Swift.min(3, labs(Int(failedO)))))")
         failedO -= Float(1 << (Swift.min(2, labs(Int(failedO)))))
      for _ in 0 ..< 1 {
         indicesy.append("\(2)")
      }
         indicesy.append("\(weak_e6C ^ 1)")
      for _ in 0 ..< 2 {
         aboutW = "\(weak_e6C)"
      }
          var iconv: String! = String(cString: [101,114,114,111,114,99,98,0], encoding: .utf8)!
          var safev: String! = String(cString: [101,118,101,114,121,119,104,101,114,101,0], encoding: .utf8)!
          var headv: Float = 1.0
         weak_e6C >>= Swift.min(labs(2), 1)
         iconv = "\(iconv.count - 1)"
         safev.append("\(Int(headv))")
         headv -= (Float(iconv == (String(cString:[116,0], encoding: .utf8)!) ? safev.count : iconv.count))
       var tableq: Double = 0.0
       var performv: Double = 2.0
      while (indicesy.hasSuffix("\(detaillabelE)")) {
         detaillabelE = (String(cString:[84,0], encoding: .utf8)!) == aboutW
         break
      }
      if (Double(aboutW.count) - performv) >= 1.77 || (performv - Double(aboutW.count)) >= 1.77 {
         aboutW.append("\(weak_e6C)")
      }
         weak_e6C &= Int(tableq) | Int(performv)
         tableq -= Double(aboutW.count % 1)
      imagesd = json5.count < 89 && !imagesd
      break
   }
   return json5

}





    
    override func viewDidLoad() {

         let tdltUllscreen: String! = maximumPositionDisabled(isbdingFailed:false, configPrompt:[327, 855], detailslabelYloading:[String(cString: [114,97,100,97,114,0], encoding: .utf8)!:3589.0])

      let tdltUllscreen_len = tdltUllscreen?.count ?? 0
     var b_31 = Int(tdltUllscreen_len)
     b_31 *= 46
      if tdltUllscreen == "call" {
              print(tdltUllscreen)
      }



       var orderB: String! = String(cString: [99,108,111,115,101,100,0], encoding: .utf8)!
    var briefn: String! = String(cString: [115,98,112,114,111,0], encoding: .utf8)!
   if orderB.count >= 3 {
      orderB.append("\(briefn.count - 3)")
   }

   repeat {
      orderB.append("\(briefn.count >> (Swift.min(3, orderB.count)))")
      if 138556 == orderB.count {
         break
      }
   } while (!orderB.contains("\(briefn.count)")) && (138556 == orderB.count)
        super.viewDidLoad()
   repeat {
      orderB.append("\(orderB.count)")
      if orderB.count == 2241403 {
         break
      }
   } while (briefn != String(cString:[108,0], encoding: .utf8)! && orderB != String(cString:[110,0], encoding: .utf8)!) && (orderB.count == 2241403)
        
        let heng = UICollectionViewFlowLayout()
        heng.scrollDirection = .vertical
        heng.sectionInset = UIEdgeInsets(top: 6, left: 16, bottom: 6, right: 16)
        heng.minimumInteritemSpacing = 0
        heng.minimumLineSpacing = 12
        
        self.collectionView.delegate = self
        self.collectionView.dataSource = self
        self.collectionView.backgroundColor = .clear
        self.collectionView.collectionViewLayout = heng
        
        if isCreate == true {
            self.collectionView.register(UINib(nibName: "TGraphicsCell", bundle: nil), forCellWithReuseIdentifier: "cell")
        }
        else {
            self.collectionView.register(UINib(nibName: "RBaseCell", bundle: nil), forCellWithReuseIdentifier: "cell")
        }
    }

@discardableResult
 func speakActiveInputNetworkEngineSender(remarkOss: String!, shouControll: Float) -> Float {
    var r_widthQ: String! = String(cString: [112,114,111,120,121,0], encoding: .utf8)!
    var parameter2: Int = 3
       var path_: [Any]! = [String(cString: [121,111,110,108,121,0], encoding: .utf8)!]
       var param4: String! = String(cString: [107,95,55,50,95,117,110,99,111,110,115,117,109,101,100,0], encoding: .utf8)!
       var beginM: String! = String(cString: [114,111,111,109,0], encoding: .utf8)!
         path_.append(path_.count ^ param4.count)
      while (4 <= (param4.count >> (Swift.min(labs(3), 2)))) {
         param4 = "\(param4.count | 1)"
         break
      }
       var writem: String! = String(cString: [114,101,117,115,97,98,108,101,0], encoding: .utf8)!
       var lishi3: String! = String(cString: [111,95,57,51,95,102,108,97,99,101,110,99,0], encoding: .utf8)!
      repeat {
          var selectedr: String! = String(cString: [97,108,98,117,109,115,0], encoding: .utf8)!
         beginM.append("\(param4.count % (Swift.max(2, 1)))")
         selectedr = "\(selectedr.count >> (Swift.min(3, selectedr.count)))"
         if beginM.count == 29391 {
            break
         }
      } while (beginM.count == 29391) && (writem == beginM)
       var goodsf: String! = String(cString: [110,111,110,110,117,108,108,100,101,115,116,105,110,97,116,105,111,110,95,118,95,48,0], encoding: .utf8)!
         lishi3 = "\(2 % (Swift.max(8, writem.count)))"
         writem = "\(writem.count)"
          var dateb: String! = String(cString: [101,95,55,53,95,109,98,101,100,116,108,115,0], encoding: .utf8)!
          var g_county: [String: Any]! = [String(cString: [100,111,119,110,115,97,109,112,108,101,95,122,95,57,56,0], encoding: .utf8)!:String(cString: [100,101,102,97,117,108,0], encoding: .utf8)!, String(cString: [116,117,110,110,101,108,101,100,95,53,95,51,51,0], encoding: .utf8)!:String(cString: [99,111,108,111,110,115,95,56,95,52,55,0], encoding: .utf8)!]
         param4.append("\(beginM.count)")
         dateb.append("\(1)")
         g_county["\(dateb)"] = 2
          var channelk: String! = String(cString: [97,117,116,104,101,110,116,105,99,97,116,105,111,110,0], encoding: .utf8)!
         param4.append("\(writem.count * lishi3.count)")
         channelk.append("\(channelk.count * channelk.count)")
      r_widthQ.append("\(beginM.count * r_widthQ.count)")
   repeat {
      parameter2 &= r_widthQ.count * 3
      if 2258975 == parameter2 {
         break
      }
   } while ((parameter2 & 1) > 1) && (2258975 == parameter2)
      r_widthQ = "\((r_widthQ == (String(cString:[84,0], encoding: .utf8)!) ? r_widthQ.count : parameter2))"
    var otherM: String! = String(cString: [102,105,108,109,0], encoding: .utf8)!
     let engineSearch: Bool = true
     var displayData: String! = String(cString: [98,121,116,101,95,108,95,56,52,0], encoding: .utf8)!
    var screenpressoDownmixExanded:Float = 0
    screenpressoDownmixExanded /= Float(engineSearch ? 70 : 23)

    return screenpressoDownmixExanded

}





    
    @IBAction func back(_ sender: Any) {

         let accumBlockie: Float = speakActiveInputNetworkEngineSender(remarkOss:String(cString: [105,100,99,116,108,108,109,0], encoding: .utf8)!, shouControll:7627.0)

      if accumBlockie < 11 {
             print(accumBlockie)
      }
     var _g_16 = Int(accumBlockie)
     var x_22 = 1
     let k_45 = 1
     if _g_16 > k_45 {
         _g_16 = k_45
     }
     while x_22 < _g_16 {
         x_22 += 1
          _g_16 += x_22
         break
     }



       var int_lW: [String: Any]! = [String(cString: [105,110,116,114,97,120,109,98,117,118,0], encoding: .utf8)!:827, String(cString: [112,111,112,117,112,0], encoding: .utf8)!:45]
    var j_centerO: [Any]! = [414, 130]
    var notificationS: String! = String(cString: [99,111,102,102,105,110,0], encoding: .utf8)!
   if (j_centerO.count + 3) <= 2 && (notificationS.count + j_centerO.count) <= 3 {
       var mines: String! = String(cString: [101,120,99,101,112,116,0], encoding: .utf8)!
       var keywordst: String! = String(cString: [108,97,116,101,110,99,121,0], encoding: .utf8)!
       var n_countE: String! = String(cString: [102,97,114,101,110,100,0], encoding: .utf8)!
       var pickedd: String! = String(cString: [99,97,115,116,115,0], encoding: .utf8)!
       var b_countA: String! = String(cString: [99,97,112,97,98,105,108,105,116,105,101,115,0], encoding: .utf8)!
          var class_ksg: String! = String(cString: [97,100,97,112,116,101,100,0], encoding: .utf8)!
          var hasW: String! = String(cString: [105,110,116,101,114,114,117,112,116,0], encoding: .utf8)!
          var delegate_ktU: [String: Any]! = [String(cString: [109,105,100,108,0], encoding: .utf8)!:700, String(cString: [99,104,101,99,107,111,117,116,0], encoding: .utf8)!:971, String(cString: [112,112,102,108,97,103,115,0], encoding: .utf8)!:123]
         n_countE.append("\(delegate_ktU.keys.count >> (Swift.min(labs(3), 1)))")
         class_ksg.append("\(hasW.count)")
         hasW.append("\(class_ksg.count)")
         delegate_ktU[hasW] = 1 - hasW.count
         keywordst.append("\(pickedd.count & keywordst.count)")
          var instanceP: Int = 1
         keywordst = "\(1)"
         instanceP >>= Swift.min(labs(instanceP), 2)
          var sortr: Float = 3.0
         pickedd = "\(mines.count)"
         sortr += Float(1 << (Swift.min(labs(Int(sortr)), 5)))
      for _ in 0 ..< 3 {
         mines = "\(b_countA.count)"
      }
      repeat {
          var searchz: Double = 1.0
         b_countA = "\(pickedd.count)"
         searchz /= Swift.max(Double(Int(searchz) >> (Swift.min(3, labs(1)))), 2)
         if b_countA.count == 2458607 {
            break
         }
      } while (b_countA.count == 2) && (b_countA.count == 2458607)
      repeat {
         b_countA = "\(n_countE.count + pickedd.count)"
         if 3699912 == b_countA.count {
            break
         }
      } while (3699912 == b_countA.count) && (mines.count > b_countA.count)
       var nicknameV: String! = String(cString: [116,114,101,122,111,114,0], encoding: .utf8)!
       var thresholdr: String! = String(cString: [99,108,97,122,122,0], encoding: .utf8)!
          var alifastO: String! = String(cString: [111,117,116,102,105,108,101,115,0], encoding: .utf8)!
          var reusable7: Float = 2.0
          var message8: String! = String(cString: [114,101,97,100,98,121,116,101,0], encoding: .utf8)!
         b_countA.append("\(((String(cString:[67,0], encoding: .utf8)!) == pickedd ? n_countE.count : pickedd.count))")
         alifastO.append("\(message8.count)")
         reusable7 -= Float(alifastO.count | 3)
         message8.append("\(message8.count | Int(reusable7))")
      while (mines.count <= thresholdr.count) {
          var true_h8: String! = String(cString: [115,119,105,102,116,0], encoding: .utf8)!
         thresholdr.append("\(2 + n_countE.count)")
         true_h8.append("\((true_h8 == (String(cString:[50,0], encoding: .utf8)!) ? true_h8.count : true_h8.count))")
         break
      }
         n_countE.append("\(1 + nicknameV.count)")
      while (n_countE == pickedd) {
         pickedd = "\(3)"
         break
      }
         pickedd = "\(keywordst.count)"
      while (!mines.hasSuffix("\(thresholdr.count)")) {
          var valueS: [Any]! = [527, 524, 765]
          var controllerW: String! = String(cString: [105,103,110,112,111,115,116,0], encoding: .utf8)!
          var fixeds: [String: Any]! = [String(cString: [109,98,117,102,115,0], encoding: .utf8)!:889, String(cString: [101,113,117,97,116,97,98,108,101,0], encoding: .utf8)!:130]
          var delegate_w7N: Bool = false
         thresholdr.append("\(mines.count - valueS.count)")
         valueS = [(1 - (delegate_w7N ? 4 : 4))]
         controllerW.append("\(((String(cString:[115,0], encoding: .utf8)!) == controllerW ? controllerW.count : (delegate_w7N ? 2 : 5)))")
         fixeds["\(delegate_w7N)"] = fixeds.count
         break
      }
      while (!mines.contains("\(nicknameV.count)")) {
         nicknameV.append("\(n_countE.count)")
         break
      }
      j_centerO.append(int_lW.count)
   }
   for _ in 0 ..< 3 {
      j_centerO.append(3)
   }
   while ((1 ^ int_lW.keys.count) <= 4) {
       var photoq: [String: Any]! = [String(cString: [104,117,109,97,110,0], encoding: .utf8)!:8086]
       var generateF: [Any]! = [674, 709]
      while ((photoq.keys.count * generateF.count) > 3 && (photoq.keys.count * generateF.count) > 3) {
         photoq["\(generateF.count)"] = photoq.count / (Swift.max(8, generateF.count))
         break
      }
      for _ in 0 ..< 1 {
         photoq = ["\(photoq.count)": 2 | photoq.count]
      }
      for _ in 0 ..< 2 {
         photoq = ["\(photoq.count)": photoq.values.count ^ 1]
      }
      if !photoq.keys.contains("\(generateF.count)") {
         photoq["\(generateF.count)"] = photoq.values.count
      }
       var remarkU: String! = String(cString: [101,100,105,116,111,114,0], encoding: .utf8)!
      repeat {
         photoq["\(generateF.count)"] = photoq.count << (Swift.min(generateF.count, 5))
         if photoq.count == 1459326 {
            break
         }
      } while ((generateF.count + photoq.values.count) >= 4) && (photoq.count == 1459326)
      notificationS = "\(photoq.keys.count << (Swift.min(notificationS.count, 3)))"
      break
   }
      int_lW = ["\(int_lW.keys.count)": int_lW.count]

       var cellq: [Any]! = [904, 753, 814]
       var collectiony: String! = String(cString: [113,110,111,115,0], encoding: .utf8)!
       var processk: Float = 3.0
         processk /= Swift.max(Float(Int(processk)), 3)
         processk /= Swift.max(1, (Float((String(cString:[48,0], encoding: .utf8)!) == collectiony ? collectiony.count : cellq.count)))
          var b_titleb: Int = 0
          var taskZ: String! = String(cString: [105,110,111,100,101,0], encoding: .utf8)!
          var buttonB: [Any]! = [501, 275]
         cellq = [buttonB.count / (Swift.max(collectiony.count, 4))]
         b_titleb %= Swift.max(1, 3)
         taskZ = "\(2 & b_titleb)"
         buttonB.append(1 & b_titleb)
          var constraintz: [String: Any]! = [String(cString: [118,105,100,101,111,112,114,111,99,101,115,115,111,114,0], encoding: .utf8)!:[1249.0]]
          var dataS: String! = String(cString: [116,105,109,115,116,97,109,112,0], encoding: .utf8)!
          var yhlogoA: Double = 3.0
         processk *= Float(Int(processk))
         constraintz["\(dataS)"] = 3
         dataS = "\(2 << (Swift.min(2, dataS.count)))"
         yhlogoA += Double(Int(yhlogoA) % (Swift.max(2, 8)))
      if collectiony.contains("\(cellq.count)") {
         cellq = [2 | cellq.count]
      }
         cellq.append(cellq.count)
      while ((cellq.count ^ 1) > 5) {
         cellq.append(1 >> (Swift.min(5, cellq.count)))
         break
      }
         processk += (Float((String(cString:[114,0], encoding: .utf8)!) == collectiony ? Int(processk) : collectiony.count))
      while ((processk + 2.100) < 2.18 && (2.100 + processk) < 2.45) {
          var otherz: Double = 4.0
          var window_7Z: Double = 1.0
          var numM: Bool = true
         processk /= Swift.max((Float(Int(window_7Z) << (Swift.min(1, labs((numM ? 4 : 5)))))), 3)
         otherz += Double(Int(otherz))
         window_7Z += Double(1)
         break
      }
      notificationS.append("\(notificationS.count % (Swift.max(8, cellq.count)))")
      int_lW = ["\(int_lW.values.count)": j_centerO.count]
        navigationController?.popViewController(animated: true)
    }

@discardableResult
 func dismissMagnitudeLatestResponse(listenPhoto: Bool) -> String! {
    var shouq: Bool = true
    var recordingvz: String! = String(cString: [114,101,115,111,108,118,105,110,103,0], encoding: .utf8)!
   while (!shouq || recordingvz.count <= 1) {
      shouq = recordingvz.count >= 46
      break
   }
   for _ in 0 ..< 1 {
      recordingvz = "\(1 * recordingvz.count)"
   }
   for _ in 0 ..< 1 {
       var lengthx: Double = 0.0
       var btnn: [Any]! = [UILabel()]
         lengthx /= Swift.max(1, Double(3 * Int(lengthx)))
      while ((3.54 * lengthx) >= 3.54) {
         btnn = [1 << (Swift.min(labs(Int(lengthx)), 4))]
         break
      }
         btnn = [Int(lengthx)]
      repeat {
         btnn.append(btnn.count / (Swift.max(1, 5)))
         if 3863105 == btnn.count {
            break
         }
      } while (3863105 == btnn.count) && (!btnn.contains { $0 as? Double == lengthx })
       var chatW: Double = 0.0
      repeat {
         lengthx -= Double(btnn.count / (Swift.max(1, 5)))
         if lengthx == 216553.0 {
            break
         }
      } while (lengthx == 216553.0) && (1.83 <= (lengthx / 5.10) || 5.10 <= (lengthx / (Swift.max(3, Double(btnn.count)))))
      shouq = recordingvz == (String(cString:[118,0], encoding: .utf8)!)
   }
   if recordingvz.hasSuffix("\(shouq)") {
      shouq = !recordingvz.hasPrefix("\(shouq)")
   }
   return recordingvz

}





    
    func search() {

         let districtEscape: String! = dismissMagnitudeLatestResponse(listenPhoto:true)

      let districtEscape_len = districtEscape?.count ?? 0
     var b_89 = Int(districtEscape_len)
     if b_89 > 179 {
          }
     else if b_89 > 733 {
          b_89 += 74

     }
      print(districtEscape)



       var reset8: Int = 5
    var statuslabelF: Double = 2.0
   for _ in 0 ..< 3 {
      statuslabelF -= Double(Int(statuslabelF) + reset8)
   }

      reset8 %= Swift.max(Int(statuslabelF) << (Swift.min(4, labs(3))), 2)
        var responder = [String: Any]()
       var uploadl: Double = 2.0
          var applyg: Float = 5.0
          var awakeI: [Any]! = [false]
         uploadl -= Double(2)
         applyg -= Float(3)
         awakeI.append(awakeI.count)
         uploadl *= Double(1)
         uploadl *= Double(Int(uploadl) >> (Swift.min(3, labs(3))))
      reset8 += Int(uploadl) | Int(statuslabelF)
        responder["aiName"] = searchTF.text!
        responder["rows"] = 60
       var k_centerz: Float = 1.0
       var resumeM: Bool = false
       var bodyu: String! = String(cString: [115,97,109,105,0], encoding: .utf8)!
      if !resumeM {
         bodyu.append("\(bodyu.count)")
      }
      for _ in 0 ..< 1 {
         resumeM = k_centerz <= 67.72
      }
         k_centerz += Float(Int(k_centerz))
      for _ in 0 ..< 2 {
          var pressz: String! = String(cString: [111,115,116,114,105,110,103,115,116,114,101,97,109,0], encoding: .utf8)!
          var candidateU: String! = String(cString: [108,97,117,110,99,104,105,110,103,0], encoding: .utf8)!
          var code7: String! = String(cString: [108,111,99,107,115,99,114,101,101,110,0], encoding: .utf8)!
          var dictionaryT: String! = String(cString: [116,114,97,110,115,105,116,105,111,110,0], encoding: .utf8)!
         resumeM = 12.2 == k_centerz
         pressz.append("\(candidateU.count / (Swift.max(9, pressz.count)))")
         candidateU = "\(code7.count)"
         code7 = "\(1)"
         dictionaryT.append("\(3)")
      }
         bodyu.append("\(bodyu.count)")
      while (resumeM) {
         resumeM = 79.1 < k_centerz || resumeM
         break
      }
       var home_: Bool = true
      for _ in 0 ..< 1 {
         home_ = !resumeM
      }
         resumeM = home_
      statuslabelF += Double(2 + Int(k_centerz))
        if isCreate == true {
            responder["aiType"] = "2"
        }
        else {
            responder["aiType"] = "1"
        }
        
        DXAlamofireMine.shared.post(urlSuffix: "/ai/findSearchAi", body: responder) { (result: Result<VPopupText, ZFMenuScreenI>) in
            switch result {
            case.success(let model):
                
                if model.code == 200 {
                    self.classItems = model.rows ?? []
                    self.collectionView.reloadData()
                    
                    if self.classItems.count == 0 {
                        self.bankView.isHidden = false
                        ZKProgressHUD.showMessage("robotlogoStarkapGundong")
                    }
                    else {
                        self.bankView.isHidden = true
                    }
                }
                else {
                    SVProgressHUD.showError(withStatus: model.msg)
                }
                break
            case.failure(_):
                SVProgressHUD.showError(withStatus: "接口请求错误");
                break
            }
        }
    }


    @IBAction func searchClick(_ sender: Any) {
       var total5: String! = String(cString: [116,106,98,101,110,99,104,116,101,115,116,0], encoding: .utf8)!
    var listA: Bool = false
    var recordingT: Double = 2.0
       var applicationD: Double = 5.0
       var next1: Double = 1.0
          var rowL: String! = String(cString: [117,110,107,110,111,119,110,0], encoding: .utf8)!
         next1 /= Swift.max(Double(3), 3)
         rowL = "\(rowL.count + 2)"
         applicationD -= Double(Int(applicationD) << (Swift.min(4, labs(Int(next1)))))
          var ylabelt: Float = 4.0
          var graphicsJ: String! = String(cString: [99,97,99,104,101,100,107,101,121,115,0], encoding: .utf8)!
          var sendH: String! = String(cString: [114,101,100,105,114,101,99,116,0], encoding: .utf8)!
         next1 += Double(sendH.count)
         ylabelt -= Float(3 & Int(ylabelt))
         graphicsJ = "\(3 & graphicsJ.count)"
         sendH = "\(Int(ylabelt) >> (Swift.min(graphicsJ.count, 5)))"
       var progressg: String! = String(cString: [103,101,111,108,111,99,97,116,105,111,110,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         applicationD += Double(Int(next1) + Int(applicationD))
      }
         next1 += Double(Int(applicationD) / (Swift.max(Int(next1), 3)))
      listA = 81.21 >= next1

      recordingT += Double(2)
        if searchTF.text?.isEmpty == true {
            ZKProgressHUD.showError("搜索不能为空")
      recordingT *= Double(2 << (Swift.min(labs(Int(recordingT)), 3)))
            return
        }
        search()
       var parameterG: Float = 3.0
       var recordsG: String! = String(cString: [114,101,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!
       var lastS: Double = 0.0
      repeat {
         parameterG /= Swift.max(Float(Int(lastS) - recordsG.count), 5)
         if parameterG == 3251286.0 {
            break
         }
      } while (2.18 < (4.83 / (Swift.max(10, parameterG)))) && (parameterG == 3251286.0)
      while (5.4 == (4.38 + lastS) || (4.38 + lastS) == 1.76) {
         lastS += Double(Int(parameterG))
         break
      }
       var nicknamer: String! = String(cString: [114,101,115,116,97,107,101,0], encoding: .utf8)!
       var freeF: String! = String(cString: [105,110,100,101,111,100,97,116,97,0], encoding: .utf8)!
      repeat {
          var window_h7: Double = 5.0
          var heady: [Any]! = [String(cString: [109,111,116,105,111,110,115,101,97,114,99,104,0], encoding: .utf8)!, String(cString: [113,117,111,116,101,100,0], encoding: .utf8)!]
          var not_5bQ: Float = 0.0
          var completion4: String! = String(cString: [97,114,99,104,105,118,101,100,0], encoding: .utf8)!
         freeF.append("\(Int(not_5bQ))")
         window_h7 += Double(completion4.count ^ 1)
         heady = [2 & heady.count]
         completion4 = "\(3 ^ Int(window_h7))"
         if (String(cString:[98,51,48,51,54,0], encoding: .utf8)!) == freeF {
            break
         }
      } while (recordsG == String(cString:[69,0], encoding: .utf8)!) && ((String(cString:[98,51,48,51,54,0], encoding: .utf8)!) == freeF)
      repeat {
          var yloadingU: String! = String(cString: [111,118,101,114,114,105,100,101,110,0], encoding: .utf8)!
          var eventW: Int = 0
          var homeg: Double = 0.0
          var ringy: String! = String(cString: [103,101,116,112,116,114,0], encoding: .utf8)!
          var gestureJ: Bool = true
         lastS += (Double(ringy == (String(cString:[101,0], encoding: .utf8)!) ? Int(homeg) : ringy.count))
         yloadingU = "\(((String(cString:[81,0], encoding: .utf8)!) == yloadingU ? eventW : yloadingU.count))"
         eventW |= 1
         homeg -= Double(eventW)
         gestureJ = !gestureJ
         if lastS == 2181426.0 {
            break
         }
      } while ((Int(lastS) * nicknamer.count) > 1 && 2.10 > (lastS * Double(nicknamer.count))) && (lastS == 2181426.0)
      while ((parameterG * Float(recordsG.count)) < 1.81 && 5 < (5 * Int(parameterG))) {
         recordsG.append("\(Int(parameterG) * freeF.count)")
         break
      }
          var serviceF: String! = String(cString: [115,108,105,112,112,97,103,101,0], encoding: .utf8)!
          var socketH: Double = 0.0
          var dataP: Int = 0
         recordsG = "\(3)"
         serviceF.append("\(Int(socketH))")
         socketH += Double(dataP << (Swift.min(labs(3), 1)))
         dataP <<= Swift.min(labs(1 * Int(socketH)), 4)
      for _ in 0 ..< 3 {
         lastS *= Double(freeF.count & recordsG.count)
      }
       var keywords1: String! = String(cString: [104,97,100,97,109,97,114,100,120,0], encoding: .utf8)!
      total5 = "\(2)"
        
        searchTF.resignFirstResponder()
      total5 = "\(3 % (Swift.max(3, total5.count)))"
    }
    
    
}

extension RMainController: UICollectionViewDelegate, UICollectionViewDataSource, UICollectionViewDelegateFlowLayout {

@discardableResult
 func querySupportPercentButton() -> UIButton! {
    var document1: String! = String(cString: [102,108,97,99,100,115,112,0], encoding: .utf8)!
    var remarkk: Double = 1.0
   repeat {
       var alamofireU: String! = String(cString: [115,111,114,101,115,101,114,118,101,0], encoding: .utf8)!
       var reusable9: [Any]! = [100, 944]
         alamofireU.append("\(alamofireU.count * reusable9.count)")
      repeat {
          var obj4: [String: Any]! = [String(cString: [112,97,114,115,105,110,103,0], encoding: .utf8)!:404, String(cString: [102,116,118,110,115,0], encoding: .utf8)!:364]
          var attributedW: String! = String(cString: [97,100,97,112,116,97,116,105,111,110,115,101,116,0], encoding: .utf8)!
         reusable9 = [1]
         obj4 = ["\(obj4.keys.count)": obj4.values.count]
         attributedW.append("\(obj4.values.count % (Swift.max(attributedW.count, 6)))")
         if 3372261 == reusable9.count {
            break
         }
      } while ((1 ^ alamofireU.count) == 1 && 1 == (alamofireU.count ^ reusable9.count)) && (3372261 == reusable9.count)
       var reusablex: [Any]! = [String(cString: [98,101,116,97,0], encoding: .utf8)!, String(cString: [114,101,112,114,101,115,101,110,116,97,98,108,101,0], encoding: .utf8)!, String(cString: [100,101,115,104,97,107,101,0], encoding: .utf8)!]
       var networki: [Any]! = [540, 226, 500]
       var selecti: [Any]! = [255, 957]
      if (reusablex.count % (Swift.max(9, networki.count))) >= 4 {
         reusablex.append(1 | reusablex.count)
      }
         networki.append(2 << (Swift.min(5, alamofireU.count)))
      remarkk -= (Double(document1 == (String(cString:[113,0], encoding: .utf8)!) ? document1.count : Int(remarkk)))
      if 1694534.0 == remarkk {
         break
      }
   } while ((document1.count - Int(remarkk)) < 3) && (1694534.0 == remarkk)
      remarkk -= Double(2 << (Swift.min(5, labs(Int(remarkk)))))
      document1 = "\(3)"
     let areaStart: String! = String(cString: [97,114,105,116,104,0], encoding: .utf8)!
     var configurationFixed: UIButton! = UIButton()
     let recordingHeight: [Any]! = [66, 338]
    var vfilterLogged:UIButton! = UIButton()
    configurationFixed.frame = CGRect(x: 189, y: 60, width: 0, height: 0)
    configurationFixed.alpha = 0.7;
    configurationFixed.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    configurationFixed.setImage(UIImage(named:String(cString: [105,109,103,0], encoding: .utf8)!), for: .normal)
    configurationFixed.setTitle("", for: .normal)
    configurationFixed.setBackgroundImage(UIImage(named:String(cString: [114,101,99,111,103,110,105,116,105,111,110,0], encoding: .utf8)!), for: .normal)
    configurationFixed.titleLabel?.font = UIFont.systemFont(ofSize:11)
    
    var configurationFixedFrame = configurationFixed.frame
    configurationFixedFrame.size = CGSize(width: 244, height: 56)
    configurationFixed.frame = configurationFixedFrame
    if configurationFixed.alpha > 0.0 {
         configurationFixed.alpha = 0.0
    }
    if configurationFixed.isHidden {
         configurationFixed.isHidden = false
    }
    if !configurationFixed.isUserInteractionEnabled {
         configurationFixed.isUserInteractionEnabled = true
    }

    vfilterLogged.frame = CGRect(x: 124, y: 55, width: 0, height: 0)
    vfilterLogged.alpha = 1.0;
    vfilterLogged.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    vfilterLogged.setTitle("", for: .normal)
    vfilterLogged.setBackgroundImage(UIImage(named:String(cString: [115,104,111,119,0], encoding: .utf8)!), for: .normal)
    vfilterLogged.titleLabel?.font = UIFont.systemFont(ofSize:19)
    vfilterLogged.setImage(UIImage(named:String(cString: [103,95,99,101,110,116,101,114,0], encoding: .utf8)!), for: .normal)

    
    var vfilterLoggedFrame = vfilterLogged.frame
    vfilterLoggedFrame.size = CGSize(width: 125, height: 142)
    vfilterLogged.frame = vfilterLoggedFrame
    if vfilterLogged.alpha > 0.0 {
         vfilterLogged.alpha = 0.0
    }
    if vfilterLogged.isHidden {
         vfilterLogged.isHidden = false
    }
    if !vfilterLogged.isUserInteractionEnabled {
         vfilterLogged.isUserInteractionEnabled = true
    }

    return vfilterLogged

}





    
    func collectionView(_ collectionView: UICollectionView, cellForItemAt indexPath: IndexPath) -> UICollectionViewCell {

         let harePresubmit: UIButton! = querySupportPercentButton()

      if harePresubmit != nil {
          self.view.addSubview(harePresubmit)
          let harePresubmit_tag = harePresubmit.tag
     var temp_v_67 = Int(harePresubmit_tag)
     temp_v_67 += 35
      }



       var terminateD: String! = String(cString: [112,111,115,116,112,114,111,99,114,101,115,0], encoding: .utf8)!
    var nnewsC: Double = 2.0
      terminateD.append("\(1)")

      terminateD = "\(terminateD.count | Int(nnewsC))"
        let presentObject = self.classItems[indexPath.row]
      terminateD = "\(3)"
        
        if isCreate == true {
            let notificationCell = collectionView.dequeueReusableCell(withReuseIdentifier: "cell", for: indexPath) as! TGraphicsCell
      terminateD.append("\(((String(cString:[117,0], encoding: .utf8)!) == terminateD ? terminateD.count : Int(nnewsC)))")
            notificationCell.backgroundColor = UIColor(red: 34/255, green: 34/255, blue: 50/255, alpha: 1.0)
            notificationCell.label.text = presentObject.aiName
            
            return notificationCell
        }

        else {
            let notificationCell = collectionView.dequeueReusableCell(withReuseIdentifier: "cell", for: indexPath) as! RBaseCell
            notificationCell.backgroundColor = UIColor(red: 34/255, green: 34/255, blue: 50/255, alpha: 1.0)
            notificationCell.label.text = presentObject.aiName
            notificationCell.desclabel.text = presentObject.aiDetails
            if let url = presentObject.headUrl {
                notificationCell.iconImage.sd_setImage(with: URL(string: url))
            }

            return notificationCell
        }
        
    }

    
    func collectionView(_ collectionView: UICollectionView, layout collectionViewLayout: UICollectionViewLayout, sizeForItemAt indexPath: IndexPath) -> CGSize {
       var headx: Double = 4.0
    var verityy: Double = 4.0
    var controlla: Double = 2.0
      controlla += Double(Int(verityy) / (Swift.max(Int(headx), 8)))
   while (1.9 >= (1.17 + headx)) {
      headx *= Double(2 >> (Swift.min(labs(Int(verityy)), 5)))
      break
   }

      verityy /= Swift.max(3, Double(Int(controlla)))
       var indexW: [Any]! = [String(cString: [112,114,111,116,101,99,116,111,114,0], encoding: .utf8)!, String(cString: [115,101,114,105,97,108,105,122,101,0], encoding: .utf8)!]
      for _ in 0 ..< 1 {
         indexW = [indexW.count]
      }
         indexW.append(1)
         indexW.append(indexW.count ^ indexW.count)
      verityy *= Double(Int(headx))
        if isCreate == true {
            return CGSize(width: (self.view.frame.size.width - 44.3)/2, height: 234)
        }else {
            return CGSize(width: (self.view.frame.size.width - 46.3)/2, height: 80)
        }
      verityy /= Swift.max(Double(3 - Int(verityy)), 5)
       var dicty: [Any]! = [182, 619]
       var homep: String! = String(cString: [99,111,109,112,108,101,116,101,100,0], encoding: .utf8)!
       var listY: String! = String(cString: [97,99,107,110,111,119,108,101,100,103,101,0], encoding: .utf8)!
       var heng4: Bool = true
       var removeY: [String: Any]! = [String(cString: [114,101,97,99,116,0], encoding: .utf8)!:456, String(cString: [111,118,101,114,118,105,101,119,0], encoding: .utf8)!:494, String(cString: [101,110,116,114,105,101,115,0], encoding: .utf8)!:8]
       var file8: [String: Any]! = [String(cString: [104,108,105,110,101,0], encoding: .utf8)!:389, String(cString: [109,101,100,105,97,110,0], encoding: .utf8)!:119, String(cString: [114,101,108,97,116,101,100,0], encoding: .utf8)!:20]
      for _ in 0 ..< 1 {
         heng4 = 65 <= homep.count || dicty.count <= 65
      }
      repeat {
         dicty.append(homep.count ^ 2)
         if 412104 == dicty.count {
            break
         }
      } while (!heng4) && (412104 == dicty.count)
          var avator8: Bool = false
         removeY["\(heng4)"] = removeY.values.count
         avator8 = (!avator8 ? !avator8 : avator8)
      for _ in 0 ..< 3 {
          var rmblabel8: [Any]! = [String(cString: [114,101,99,101,110,116,108,121,0], encoding: .utf8)!, String(cString: [119,114,105,116,101,120,0], encoding: .utf8)!]
         removeY["\(rmblabel8.count)"] = rmblabel8.count >> (Swift.min(labs(3), 2))
      }
         file8[listY] = file8.count
      while ((file8.keys.count / 1) > 2 && heng4) {
         heng4 = listY.count >= 38
         break
      }
      repeat {
          var update_mQ: String! = String(cString: [109,117,116,97,116,105,110,103,0], encoding: .utf8)!
          var timelabelJ: Int = 1
         homep = "\(timelabelJ & listY.count)"
         update_mQ = "\(update_mQ.count)"
         timelabelJ -= update_mQ.count
         if homep.count == 3212947 {
            break
         }
      } while (!heng4 && 2 == homep.count) && (homep.count == 3212947)
      verityy /= Swift.max(5, Double(1 - Int(headx)))
    }

    func collectionView(_ collectionView: UICollectionView, numberOfItemsInSection section: Int) -> Int {
       var playd: Float = 0.0
    var avatorA: String! = String(cString: [100,114,97,119,101,114,0], encoding: .utf8)!
       var time_yyC: Double = 0.0
         time_yyC /= Swift.max(Double(3), 2)
      while (5.57 >= time_yyC) {
         time_yyC += Double(Int(time_yyC))
         break
      }
         time_yyC *= Double(3 * Int(time_yyC))
      avatorA.append("\(((String(cString:[107,0], encoding: .utf8)!) == avatorA ? avatorA.count : Int(time_yyC)))")
   while (Int(playd) > avatorA.count) {
      avatorA = "\(2)"
      break
   }
      avatorA.append("\(2 ^ Int(playd))")

   repeat {
       var dateu: Bool = true
       var sendh: Double = 3.0
       var press4: String! = String(cString: [110,111,110,98,108,111,99,107,0], encoding: .utf8)!
         dateu = 88.24 > sendh
      for _ in 0 ..< 1 {
         sendh -= (Double(Int(sendh) | (dateu ? 4 : 1)))
      }
         dateu = !dateu && sendh <= 57.94
         press4 = "\(Int(sendh))"
       var launchf: Bool = false
         press4.append("\(((dateu ? 2 : 3)))")
         press4.append("\((2 >> (Swift.min(labs((launchf ? 3 : 2)), 1))))")
         dateu = (!dateu ? !launchf : dateu)
      repeat {
          var firstI: Double = 3.0
          var instancea: [String: Any]! = [String(cString: [101,113,117,105,118,97,108,101,110,99,101,0], encoding: .utf8)!:String(cString: [113,119,111,114,100,0], encoding: .utf8)!, String(cString: [117,110,114,101,103,105,115,116,101,114,0], encoding: .utf8)!:String(cString: [115,117,98,116,108,101,0], encoding: .utf8)!, String(cString: [102,97,115,116,0], encoding: .utf8)!:String(cString: [114,111,116,114,0], encoding: .utf8)!]
         dateu = firstI == 73.40 || dateu
         firstI /= Swift.max(Double(instancea.values.count * instancea.values.count), 3)
         if dateu ? !dateu : dateu {
            break
         }
      } while (!dateu && (sendh + 1.2) < 1.42) && (dateu ? !dateu : dateu)
      avatorA = "\(press4.count)"
      if avatorA.count == 4466383 {
         break
      }
   } while ((Int(playd) / (Swift.max(5, 6))) <= 5 && (Int(playd) / (Swift.max(6, avatorA.count))) <= 5) && (avatorA.count == 4466383)
        return self.classItems.count
    }

    
    func collectionView(_ collectionView: UICollectionView, didSelectItemAt indexPath: IndexPath) {
       var unselecteda: String! = String(cString: [112,109,107,0], encoding: .utf8)!
    var unselected6: String! = String(cString: [97,100,115,103,97,115,0], encoding: .utf8)!
   if unselecteda != String(cString:[105,0], encoding: .utf8)! {
       var icon9: [String: Any]! = [String(cString: [106,100,115,97,109,112,108,101,0], encoding: .utf8)!:String(cString: [115,104,97,114,112,101,110,0], encoding: .utf8)!, String(cString: [97,108,112,104,97,101,120,116,114,97,99,116,0], encoding: .utf8)!:String(cString: [118,101,114,116,105,99,97,108,108,121,0], encoding: .utf8)!]
       var homek: Double = 0.0
         homek += Double(icon9.keys.count & Int(homek))
      while (2.21 >= homek) {
          var cleanb: String! = String(cString: [103,97,105,110,99,0], encoding: .utf8)!
         icon9["\(homek)"] = icon9.count
         cleanb = "\(cleanb.count + cleanb.count)"
         break
      }
      if 1.18 == (Double(icon9.count) * homek) {
         homek += Double(Int(homek))
      }
      while ((Int(homek) - icon9.values.count) < 4) {
         icon9 = ["\(icon9.values.count)": icon9.values.count]
         break
      }
         homek /= Swift.max(3, Double(3 + Int(homek)))
         icon9["\(homek)"] = icon9.values.count & Int(homek)
      unselected6 = "\(((String(cString:[71,0], encoding: .utf8)!) == unselecteda ? unselecteda.count : unselected6.count))"
   }

   for _ in 0 ..< 2 {
       var speed3: Bool = false
       var register_98: String! = String(cString: [108,105,110,117,120,0], encoding: .utf8)!
       var recordingvd: Bool = false
       var executeG: Float = 0.0
          var keyA: Bool = true
         speed3 = executeG == 1.31 || !keyA
      for _ in 0 ..< 2 {
         recordingvd = (Int(executeG) / (Swift.max(register_98.count, 3))) >= 46
      }
      for _ in 0 ..< 3 {
         register_98 = "\(register_98.count)"
      }
      while (recordingvd || speed3) {
         speed3 = !recordingvd
         break
      }
      while ((executeG - 2.65) < 5.71 && recordingvd) {
         recordingvd = register_98.count <= 88
         break
      }
      for _ in 0 ..< 1 {
         register_98.append("\(((speed3 ? 1 : 4) / (Swift.max(Int(executeG), 2))))")
      }
      if !register_98.hasPrefix("\(recordingvd)") {
         register_98.append("\(((speed3 ? 1 : 1) % (Swift.max(register_98.count, 10))))")
      }
      for _ in 0 ..< 2 {
         register_98.append("\((register_98 == (String(cString:[78,0], encoding: .utf8)!) ? register_98.count : Int(executeG)))")
      }
         recordingvd = executeG <= 56.81 && recordingvd
         register_98.append("\(Int(executeG) / 2)")
      while (4 <= register_98.count) {
          var handlerU: [Any]! = [846, 544]
          var w_height2: [Any]! = [[String(cString: [105,110,99,111,109,112,108,101,116,101,0], encoding: .utf8)!:235, String(cString: [113,101,120,112,110,101,103,0], encoding: .utf8)!:375]]
          var stylej: Bool = false
         register_98 = "\(w_height2.count)"
         handlerU = [handlerU.count << (Swift.min(labs(3), 2))]
         w_height2.append(1)
         break
      }
      if !speed3 {
         executeG += Float(2)
      }
      unselected6.append("\(((recordingvd ? 2 : 5)))")
   }
        let presentObject = self.classItems[indexPath.row]
      unselecteda.append("\(unselecteda.count + unselected6.count)")
        let task = UXCollectionTextController()
   while (unselecteda != unselected6) {
      unselected6 = "\(unselecteda.count - 1)"
      break
   }
        task.isChat = true
        task.typeID = presentObject.id!
        task.aiName = presentObject.aiName!
        task.aiDetails = presentObject.aiDetails!
        task.hidesBottomBarWhenPushed = true
        self.navigationController?.pushViewController(task, animated: true)
    }
    
}
