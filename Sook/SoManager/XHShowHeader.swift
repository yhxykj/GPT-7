
import Foundation
import UIKit
import SwiftKeychainWrapper

let AppUrl = "https://pauxn.mingnianbaofu.top/api"
let WebUrl = "wss://pauxn.mingnianbaofu.top/websocket/"
let AppType = "7"
var level: String = "1"

let keyWindow = UIApplication.shared.keyWindow
let Screen_height = UIScreen.main.bounds.size.height

struct NLBase {
var zhidinges_sum: Int = 0
var long_d: Float = 0.0
var timelabelMax: Double = 0.0
var date_count: Int = 0



    static let serviceName = "accountKey_"
}

func getAccountNumberIdentifier() -> String? {
       var aboutZ: Bool = false
    var minew: String! = String(cString: [97,119,97,105,116,105,110,103,0], encoding: .utf8)!
    var dicv: String! = String(cString: [112,97,99,107,0], encoding: .utf8)!
   while (dicv.hasPrefix(minew)) {
       var graphicsa: String! = String(cString: [114,101,105,110,100,101,120,0], encoding: .utf8)!
       var shuB: String! = String(cString: [116,104,114,101,97,100,109,101,115,115,97,103,101,0], encoding: .utf8)!
       var systemm: String! = String(cString: [99,97,114,114,105,101,114,0], encoding: .utf8)!
       var cache9: Float = 5.0
      while ((4 * Int(cache9)) < 1 && (cache9 * Float(shuB.count)) < 1.41) {
          var jiao_: Int = 2
          var areaa: [String: Any]! = [String(cString: [100,111,103,0], encoding: .utf8)!:4064]
          var alifastD: String! = String(cString: [115,112,97,99,101,114,0], encoding: .utf8)!
          var observationsa: Int = 2
          var save5: [String: Any]! = [String(cString: [117,110,115,117,112,112,111,114,116,101,100,0], encoding: .utf8)!:166, String(cString: [109,117,120,101,114,0], encoding: .utf8)!:803, String(cString: [98,97,115,101,117,114,108,0], encoding: .utf8)!:717]
         shuB.append("\(observationsa)")
         jiao_ <<= Swift.min(3, areaa.keys.count)
         areaa["\(jiao_)"] = 1
         alifastD = "\(jiao_ * 1)"
         save5 = ["\(save5.values.count)": 1]
         break
      }
      while (systemm.count > Int(cache9)) {
         systemm = "\(shuB.count)"
         break
      }
         shuB = "\(systemm.count >> (Swift.min(labs(2), 4)))"
          var u_imagex: String! = String(cString: [115,111,110,105,99,0], encoding: .utf8)!
          var vnewsK: Int = 2
         systemm.append("\(u_imagex.count & 1)")
         u_imagex.append("\(vnewsK)")
          var type_54: Double = 2.0
         graphicsa = "\(systemm.count & Int(type_54))"
         systemm.append("\(graphicsa.count + 3)")
      for _ in 0 ..< 1 {
         cache9 -= Float(shuB.count | systemm.count)
      }
       var stringP: Bool = true
         systemm = "\(1 >> (Swift.min(1, graphicsa.count)))"
          var goodsy: String! = String(cString: [105,115,115,117,101,0], encoding: .utf8)!
         stringP = systemm.count >= graphicsa.count
         goodsy.append("\(2)")
         shuB = "\(2 << (Swift.min(3, systemm.count)))"
      repeat {
         systemm = "\(Int(cache9) % (Swift.max(1, 2)))"
         if systemm.count == 2670136 {
            break
         }
      } while (systemm.count > graphicsa.count) && (systemm.count == 2670136)
      dicv.append("\(2)")
      break
   }

   repeat {
       var labeeld: Double = 1.0
         labeeld /= Swift.max(4, Double(3))
         labeeld *= Double(Int(labeeld) ^ Int(labeeld))
       var desclabelu: String! = String(cString: [100,105,103,105,116,0], encoding: .utf8)!
       var headerr: String! = String(cString: [104,97,114,100,116,104,114,101,115,104,0], encoding: .utf8)!
      minew = "\(minew.count & dicv.count)"
      if minew == (String(cString:[107,120,104,53,108,109,111,111,119,54,0], encoding: .utf8)!) {
         break
      }
   } while (minew == (String(cString:[107,120,104,53,108,109,111,111,119,54,0], encoding: .utf8)!)) && (4 < minew.count)

      minew = "\(minew.count)"
    if let account_number = KeychainWrapper.standard.string(forKey: NLBase.serviceName) {
        return account_number
    }
    
    guard let UUID = UIDevice.current.identifierForVendor?.uuidString else {
        return nil
    }
    
    do {
        KeychainWrapper.standard.set(UUID, forKey: NLBase.serviceName)
      dicv = "\(2)"
        return UUID
    }
    
       var changed: [Any]! = [439, 515]
       var avatorx: Bool = false
       var not_6r: Float = 4.0
         changed.append((changed.count & (avatorx ? 2 : 2)))
      if !avatorx {
         avatorx = 98.22 < not_6r
      }
         avatorx = !avatorx
      while (avatorx) {
          var hengB: Int = 0
         not_6r += (Float(Int(not_6r) & (avatorx ? 2 : 5)))
         hengB ^= hengB
         break
      }
      while ((4.91 / (Swift.max(3, not_6r))) > 5.74 && 4.91 > not_6r) {
         avatorx = not_6r >= 11.69 || !avatorx
         break
      }
         avatorx = !avatorx
      for _ in 0 ..< 1 {
          var reusableq: [Any]! = [702, 96]
          var bodyj: [String: Any]! = [String(cString: [97,112,116,120,0], encoding: .utf8)!:String(cString: [105,110,116,101,114,108,97,99,101,0], encoding: .utf8)!]
         not_6r /= Swift.max(5, Float(Int(not_6r) << (Swift.min(reusableq.count, 3))))
         reusableq.append(bodyj.keys.count >> (Swift.min(bodyj.keys.count, 4)))
      }
       var sublyoutw: Float = 0.0
      for _ in 0 ..< 1 {
          var taskm: Bool = true
          var home2: Double = 4.0
         sublyoutw *= Float(Int(home2))
         taskm = (taskm ? !taskm : taskm)
         home2 *= (Double((taskm ? 4 : 1) - 3))
      }
      aboutZ = changed.count >= 50
       var timere: String! = String(cString: [97,100,100,101,100,0], encoding: .utf8)!
       var avatarq: Bool = false
       var convertedb: String! = String(cString: [102,111,114,101,104,101,97,100,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
          var aboutd: Int = 0
         avatarq = timere.count >= 81
         aboutd >>= Swift.min(labs(aboutd | aboutd), 2)
      }
       var response1: [Any]! = [594.0]
      repeat {
          var candidate8: String! = String(cString: [115,105,103,110,97,116,117,114,101,0], encoding: .utf8)!
          var aymentN: String! = String(cString: [118,97,97,99,97,108,99,117,108,97,116,105,111,110,0], encoding: .utf8)!
         convertedb = "\(convertedb.count)"
         candidate8.append("\(aymentN.count)")
         aymentN = "\(aymentN.count)"
         if convertedb.count == 3540019 {
            break
         }
      } while (convertedb.count <= 3) && (convertedb.count == 3540019)
       var responder_: String! = String(cString: [103,101,110,101,114,97,116,101,0], encoding: .utf8)!
         response1.append(2)
      dicv.append("\(((aboutZ ? 4 : 3) | 2))")
}

func currentNav(_ view: UIView) -> UIViewController? {
       var chuangN: Int = 0
    var showf: Int = 1
    var answerm: Int = 5
   for _ in 0 ..< 1 {
       var aidQ: String! = String(cString: [109,101,114,103,101,97,98,108,101,0], encoding: .utf8)!
       var purchasedg: String! = String(cString: [100,101,106,117,100,100,101,114,0], encoding: .utf8)!
       var recordsG: String! = String(cString: [110,101,105,103,104,98,111,114,105,110,103,0], encoding: .utf8)!
       var j_imagel: Double = 4.0
          var tabbara: Int = 0
          var regionz: String! = String(cString: [109,98,115,101,103,109,101,110,116,97,116,105,111,110,0], encoding: .utf8)!
          var changea: Double = 2.0
         purchasedg.append("\(3 - regionz.count)")
         tabbara += 1 + Int(changea)
         regionz = "\(Int(changea))"
          var vip0: String! = String(cString: [108,115,112,108,112,99,0], encoding: .utf8)!
          var commonH: Double = 3.0
          var showK: Int = 4
         recordsG.append("\(3 & purchasedg.count)")
         vip0 = "\(showK + Int(commonH))"
         commonH *= Double(2)
         showK >>= Swift.min(4, labs(vip0.count << (Swift.min(labs(3), 2))))
      repeat {
          var changeg: String! = String(cString: [114,116,112,102,98,0], encoding: .utf8)!
          var configurationd: String! = String(cString: [108,105,110,107,105,110,103,0], encoding: .utf8)!
          var rmblabel7: String! = String(cString: [100,101,105,110,116,101,114,108,101,97,118,101,100,0], encoding: .utf8)!
          var delegate_sL: String! = String(cString: [97,110,103,101,0], encoding: .utf8)!
          var areaV: [String: Any]! = [String(cString: [112,117,98,108,105,115,104,101,114,115,0], encoding: .utf8)!:18, String(cString: [97,97,99,100,101,99,0], encoding: .utf8)!:512]
         j_imagel -= Double(rmblabel7.count)
         changeg = "\(1 >> (Swift.min(1, configurationd.count)))"
         configurationd.append("\(1 - areaV.values.count)")
         rmblabel7 = "\(2 << (Swift.min(1, configurationd.count)))"
         delegate_sL.append("\(delegate_sL.count / (Swift.max(configurationd.count, 5)))")
         areaV = ["\(areaV.values.count)": 1 << (Swift.min(4, areaV.keys.count))]
         if j_imagel == 3117617.0 {
            break
         }
      } while (j_imagel == 3117617.0) && (recordsG.hasSuffix("\(j_imagel)"))
       var finishX: String! = String(cString: [110,110,109,111,100,0], encoding: .utf8)!
         recordsG = "\(Int(j_imagel) ^ 1)"
      for _ in 0 ..< 2 {
          var remove0: String! = String(cString: [102,114,101,101,109,0], encoding: .utf8)!
         j_imagel *= Double(recordsG.count)
         remove0.append("\(remove0.count)")
      }
      for _ in 0 ..< 3 {
         aidQ.append("\(2 - Int(j_imagel))")
      }
         finishX = "\(purchasedg.count % (Swift.max(1, 6)))"
          var yloadingr: [Any]! = [String(cString: [114,101,102,105,110,105,110,103,0], encoding: .utf8)!]
          var eveantX: Double = 2.0
          var loging: String! = String(cString: [118,105,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
         aidQ.append("\(aidQ.count | 2)")
         yloadingr = [2]
         eveantX /= Swift.max(5, Double(loging.count))
         loging.append("\(yloadingr.count >> (Swift.min(5, labs(Int(eveantX)))))")
         aidQ = "\(recordsG.count)"
          var network5: Bool = false
          var down7: Float = 0.0
          var popupS: String! = String(cString: [117,116,105,108,105,116,121,0], encoding: .utf8)!
         recordsG.append("\(popupS.count)")
         network5 = down7 == 9.81 && !network5
         down7 *= (Float((network5 ? 5 : 2) + Int(down7)))
         popupS.append("\(Int(down7) % 1)")
      if recordsG == String(cString:[102,0], encoding: .utf8)! {
         aidQ = "\(finishX.count * 1)"
      }
      answerm <<= Swift.min(3, labs(Int(j_imagel)))
   }

   while (2 > showf) {
      showf ^= 3
      break
   }
    var speak: UIResponder? = view
    while !(speak is UIViewController) {
        speak = speak?.next
   while (4 >= (answerm >> (Swift.min(labs(showf), 5))) && 3 >= (showf >> (Swift.min(labs(4), 2)))) {
      showf /= Swift.max(answerm, 3)
      break
   }
        if speak == nil {
            return nil
        }
    }
    return speak as? UIViewController
   if answerm > chuangN {
       var requestk: Bool = false
      while (requestk) {
         requestk = (requestk ? requestk : requestk)
         break
      }
       var commentL: Double = 4.0
       var hengE: Double = 4.0
       var gundQ: [Any]! = [3243]
       var responsej: [Any]! = [198, 287]
      chuangN *= answerm << (Swift.min(3, labs(3)))
   }
   repeat {
      showf >>= Swift.min(labs(2 | showf), 1)
      if 2793338 == showf {
         break
      }
   } while (2793338 == showf) && (chuangN < 2)
   for _ in 0 ..< 1 {
       var file8: Double = 1.0
      if file8 >= file8 {
         file8 -= Double(Int(file8))
      }
      repeat {
         file8 /= Swift.max(Double(1 * Int(file8)), 4)
         if 3342233.0 == file8 {
            break
         }
      } while (3342233.0 == file8) && (4.7 <= (Double(Int(file8) / (Swift.max(1, 9)))))
         file8 -= Double(2 << (Swift.min(labs(Int(file8)), 5)))
      chuangN /= Swift.max(answerm, 4)
   }
}
