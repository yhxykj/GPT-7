
import Foundation

import UIKit
import SVProgressHUD

struct VGKRegister: Codable {




    let msg: String?
    let code: Int?
    let data: [String: String]?
}

struct KClass: Codable {




    let msg: String?
    let code: Int?
    let data: mineModel?
}

struct mineModel: Codable {

    let vipLabel: String?
    let vipStatus: String?
    let id: String?
    let vipExpireTime: String?
    let imgNum: Int?
    let avatar: String?
    let nickname: String?
    let wx: String?
}

func mineInfo(Prefix:(() -> Void)? = nil) {
       var homef: [Any]! = [String(cString: [112,114,101,102,101,114,101,110,99,101,115,0], encoding: .utf8)!, String(cString: [114,101,102,115,0], encoding: .utf8)!]
    var userdata6: String! = String(cString: [98,111,111,107,109,97,114,107,0], encoding: .utf8)!
   if (userdata6.count | homef.count) >= 4 && 2 >= (4 | homef.count) {
       var alamofireO: Int = 4
      repeat {
          var selectedW: String! = String(cString: [100,101,115,101,108,101,99,116,0], encoding: .utf8)!
         alamofireO <<= Swift.min(selectedW.count, 5)
         if alamofireO == 2606445 {
            break
         }
      } while (alamofireO == alamofireO) && (alamofireO == 2606445)
         alamofireO &= alamofireO
      for _ in 0 ..< 1 {
         alamofireO /= Swift.max(2 + alamofireO, 5)
      }
      homef = [((String(cString:[79,0], encoding: .utf8)!) == userdata6 ? homef.count : userdata6.count)]
   }

      homef = [homef.count]
    
    SVProgressHUD.show()
    DXAlamofireMine.shared.post(urlSuffix: "/app/user/getCurrentInfo", body: [String: Any]()) { (result: Result<KClass, ZFMenuScreenI>) in
   if (userdata6.count >> (Swift.min(1, homef.count))) > 1 && (homef.count >> (Swift.min(userdata6.count, 4))) > 1 {
       var tapB: Int = 4
       var message9: Float = 0.0
       var m_view2: String! = String(cString: [110,97,118,105,103,97,116,105,111,110,0], encoding: .utf8)!
       var modelw: String! = String(cString: [100,98,108,113,117,111,116,101,0], encoding: .utf8)!
       var controlli: Bool = false
      repeat {
         m_view2 = "\(Int(message9) * 2)"
         if m_view2 == (String(cString:[116,107,104,109,57,55,118,0], encoding: .utf8)!) {
            break
         }
      } while (m_view2 == (String(cString:[116,107,104,109,57,55,118,0], encoding: .utf8)!)) && (m_view2.count < tapB)
         message9 *= Float(m_view2.count)
          var responseK: Int = 1
          var appJ: String! = String(cString: [112,111,115,116,105,111,110,0], encoding: .utf8)!
         controlli = (modelw.count - m_view2.count) >= 83
         responseK &= 3 & responseK
         appJ = "\(responseK)"
      if controlli {
         modelw.append("\(1 & Int(message9))")
      }
          var briefK: Bool = false
          var safed: Double = 4.0
         modelw = "\(((String(cString:[73,0], encoding: .utf8)!) == modelw ? (controlli ? 4 : 2) : modelw.count))"
         briefK = 13.63 >= safed
         safed *= (Double(Int(safed) / (Swift.max(5, (briefK ? 2 : 4)))))
         controlli = m_view2 == (String(cString:[85,0], encoding: .utf8)!)
      while ((5 / (Swift.max(6, tapB))) > 4) {
          var alamofirej: Double = 4.0
          var writeT: Bool = false
          var cacher: Double = 1.0
          var chat3: String! = String(cString: [103,101,116,116,105,110,103,0], encoding: .utf8)!
          var rmbu: Double = 3.0
         tapB >>= Swift.min(4, chat3.count)
         alamofirej += (Double((writeT ? 4 : 3) * Int(rmbu)))
         writeT = !writeT && 10.67 < cacher
         cacher *= Double(2 / (Swift.max(8, Int(cacher))))
         chat3.append("\(Int(alamofirej) * 2)")
         rmbu += Double(Int(cacher) - 1)
         break
      }
      while (1 == (tapB | 4)) {
          var table1: Float = 2.0
          var labeel_: String! = String(cString: [115,121,109,98,111,108,115,0], encoding: .utf8)!
          var request_: Double = 4.0
         tapB &= (labeel_ == (String(cString:[68,0], encoding: .utf8)!) ? Int(table1) : labeel_.count)
         table1 -= Float(3)
         break
      }
          var tapS: [String: Any]! = [String(cString: [114,101,108,97,121,111,117,116,0], encoding: .utf8)!:437, String(cString: [103,114,97,100,105,101,110,116,0], encoding: .utf8)!:603]
          var prefix_4ov: Bool = true
          var socketu: String! = String(cString: [99,111,115,112,105,0], encoding: .utf8)!
         message9 *= Float(2 ^ modelw.count)
         tapS = ["\(tapS.count)": 2]
         prefix_4ov = prefix_4ov || socketu.count < 66
         socketu.append("\(socketu.count)")
         controlli = (m_view2.count >> (Swift.min(modelw.count, 1))) < 62
         tapB &= modelw.count
         modelw = "\(Int(message9))"
      for _ in 0 ..< 1 {
         m_view2.append("\(1)")
      }
      if modelw.hasSuffix("\(controlli)") {
          var beginm: Bool = false
          var engined: String! = String(cString: [115,112,105,110,110,105,110,103,0], encoding: .utf8)!
          var uploadg: [String: Any]! = [String(cString: [100,114,97,103,0], encoding: .utf8)!:String(cString: [99,97,110,99,101,108,108,101,114,0], encoding: .utf8)!, String(cString: [116,114,97,110,115,0], encoding: .utf8)!:String(cString: [105,110,118,105,116,101,115,0], encoding: .utf8)!, String(cString: [117,95,50,56,0], encoding: .utf8)!:String(cString: [99,97,99,104,101,100,0], encoding: .utf8)!]
          var prefix_jJ: Double = 3.0
         modelw.append("\((1 / (Swift.max(6, (beginm ? 2 : 1)))))")
         beginm = uploadg["\(prefix_jJ)"] == nil
         engined.append("\(Int(prefix_jJ) & 1)")
         uploadg = ["\(uploadg.values.count)": 1]
      }
         m_view2 = "\((tapB & (controlli ? 1 : 4)))"
      homef = [1]
   }
        switch result {
        case.success(let model):
            
            print(model.data)
      homef = [((String(cString:[101,0], encoding: .utf8)!) == userdata6 ? userdata6.count : homef.count)]
            if model.code == 200 {
                UserDefaults.standard.set(model.data?.vipStatus, forKey: "VIP")
                UserDefaults.standard.set(model.data?.vipExpireTime, forKey: "Time")
                UserDefaults.standard.set(model.data?.nickname, forKey: "name")
                UserDefaults.standard.set(model.data?.wx, forKey: "chuangDelete_hp")
                UserDefaults.standard.set(model.data?.avatar, forKey: "avatorItems")
                
                NotificationCenter.default.post(name: NSNotification.Name("updateUserInfoNotificationName"), object: nil)
                mineFreeNumber()
                checkAliToken()
            }
            else if (model.code == 401) {
                NotificationCenter.default.post(name: NSNotification.Name("loginFailNotificationName"), object: nil)
            }
            else {
                
            }
            
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求错误");
            
            break
        }
    }
    
}


struct BVManagerManager: Codable {


    let msg: String?
    let code: Int?
}

func messageRequest(verity: String, content: String, typeId: String, imgUrl: String, isCard: Bool, Prefix:(() -> Void)? = nil) {
       var socketp: Double = 2.0
    var generateN: String! = String(cString: [102,105,110,97,108,105,115,101,114,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      socketp += Double(generateN.count)
   }

       var engineq: Float = 2.0
          var pic3: String! = String(cString: [117,110,99,108,101,115,0], encoding: .utf8)!
          var modelE: Float = 5.0
          var ypricelabelr: String! = String(cString: [100,101,99,114,101,97,115,101,0], encoding: .utf8)!
         engineq -= Float(Int(modelE))
         pic3.append("\((pic3 == (String(cString:[79,0], encoding: .utf8)!) ? ypricelabelr.count : pic3.count))")
         modelE /= Swift.max(Float(ypricelabelr.count), 2)
         engineq /= Swift.max(Float(2 | Int(engineq)), 2)
      while ((2.4 * engineq) < 2.10 || 2.43 < (engineq * 2.4)) {
          var handingf: Double = 4.0
          var ylabelB: String! = String(cString: [104,97,115,104,0], encoding: .utf8)!
          var editm: String! = String(cString: [99,105,100,0], encoding: .utf8)!
         engineq -= Float(ylabelB.count >> (Swift.min(labs(3), 3)))
         handingf -= Double(3 >> (Swift.min(1, labs(Int(handingf)))))
         ylabelB.append("\(Int(handingf) | 3)")
         editm.append("\(3 << (Swift.min(4, editm.count)))")
         break
      }
      generateN.append("\(((String(cString:[90,0], encoding: .utf8)!) == generateN ? generateN.count : Int(socketp)))")
    var responder = [String: Any]()
   if generateN.count <= 4 {
      socketp *= Double(Int(socketp) - generateN.count)
   }
    responder["prompt"] = content
      socketp -= Double(Int(socketp) - generateN.count)
    responder["uid"] = verity
    responder["aiTypeId"] = typeId
    responder["modelType"] = 0
    responder["modelId"] = level
    if imgUrl.count > 0 {
        responder["imgUrl"] = imgUrl
    }
    
    var url = "/ai/aiChat"
    if isCard == true {
        url = "/ai/xunFeiAiChatImg"
    }
    
    DXAlamofireMine.shared.post(urlSuffix: url, body: responder) { (result: Result<BVManagerManager, ZFMenuScreenI>) in
        
        switch result {
            case .success(let model):
                
            if model.code == 200 {

            }else {
                SVProgressHUD.showError(withStatus: model.msg)
            }
                break
            case .failure(_):
            
                SVProgressHUD.showError(withStatus: "接口请求错误");
                break
        }
        
    }
}

struct QTLAudioUpload: Codable {




    let msg: String?
    let code: Int?
    let data: String?
}

struct GMURegister: Codable {


    let msg: String?
    let code: Int?
    let data: JBODetails?
}

struct JBODetails: Codable {



    
    let prompt: String?
    let imgUrl: String?
    let taskType: String?
    let id: String?
    let userId: String?
    let imgUrls: [String]?
    let imgTaskId: String?
    let resultConfig: Int?
    let taskParameter: String?
    let sum: Int?
}



struct AEGLaunchManager: Codable {



    let msg: String?
    let code: Int?
    let data: [TGPhoneHeader]?
}

struct TGPhoneHeader: Codable {




    let dictLabel: String?
    let dictValue: String?
    
}

struct VPopupText: Codable {


    let msg: String?
    let code: Int?
    let rows: [RVZMine]?
}

struct RVZMine: Codable {




    let aiName: String?
    let createType: String?
    let headUrl: String?
    let id: String?
    let aiBrief: String?
    let systemType: Int?
    let aiType: Int?
    let aiDetails: String?
    let aiTypeName: String?
    let recordHead: String?
}


struct AGDetails: Codable {



    let msg: String?
    let code: Int?
    let data: String?
}

struct DLMResponse: Codable {




    let msg: String?
    let code: Int?
    let data: [EQPopup]?
}

struct EQPopup: Codable {




    let amount: String?
    let amountDescript: String?
    let descript: String?
    let id: String?
    let iosId: String?
    let mealValue: Int?
    let sort: Int?
    let status: String?
    let valueDescript: String?
    let systemType: String?
    let type: String?
    let remark: String?
    let region: String?
}

func isChatPermis() -> Bool {
       var failedm: Double = 4.0
    var commonu: [String: Any]! = [String(cString: [103,114,97,121,114,103,98,0], encoding: .utf8)!:String(cString: [105,97,116,0], encoding: .utf8)!, String(cString: [99,111,102,97,99,116,111,114,0], encoding: .utf8)!:String(cString: [112,97,115,115,0], encoding: .utf8)!]
    var speedH: Bool = true
   for _ in 0 ..< 1 {
      speedH = nil != commonu["\(failedm)"]
   }

   repeat {
       var json0: [Any]! = [94, 616, 883]
       var parametersA: [Any]! = [446, 112]
       var collectionh: Double = 2.0
       var ossR: String! = String(cString: [99,102,102,116,98,0], encoding: .utf8)!
       var speakB: String! = String(cString: [120,112,117,98,0], encoding: .utf8)!
      repeat {
          var userdatap: String! = String(cString: [116,101,114,109,105,110,97,116,111,114,0], encoding: .utf8)!
          var volumeI: String! = String(cString: [114,97,110,103,101,99,111,100,101,114,0], encoding: .utf8)!
          var remarky: Double = 5.0
          var verticalv: Int = 5
         parametersA = [verticalv]
         userdatap = "\(Int(remarky))"
         volumeI.append("\(userdatap.count)")
         remarky -= Double(volumeI.count)
         verticalv |= (volumeI == (String(cString:[48,0], encoding: .utf8)!) ? volumeI.count : Int(remarky))
         if 1625912 == parametersA.count {
            break
         }
      } while (1625912 == parametersA.count) && ((collectionh / (Swift.max(Double(parametersA.count), 10))) <= 5.18)
      if (collectionh / 2.1) > 1.29 && 5 > (parametersA.count >> (Swift.min(labs(5), 3))) {
          var while_0tI: String! = String(cString: [98,97,99,107,116,114,97,99,101,0], encoding: .utf8)!
          var formatterY: Int = 1
          var socket7: Float = 0.0
         collectionh += (Double(speakB == (String(cString:[81,0], encoding: .utf8)!) ? Int(collectionh) : speakB.count))
         while_0tI = "\(Int(socket7) & formatterY)"
         formatterY += Int(socket7)
      }
       var graphicsT: Bool = false
         ossR.append("\(json0.count | 2)")
       var navigationn: String! = String(cString: [121,117,118,112,97,99,107,101,100,0], encoding: .utf8)!
      repeat {
         graphicsT = speakB == (String(cString:[112,0], encoding: .utf8)!)
         if graphicsT ? !graphicsT : graphicsT {
            break
         }
      } while (navigationn.count > 5 || graphicsT) && (graphicsT ? !graphicsT : graphicsT)
      for _ in 0 ..< 1 {
         speakB.append("\(3 >> (Swift.min(3, labs(Int(collectionh)))))")
      }
      if !navigationn.hasPrefix("\(ossR.count)") {
         ossR = "\(parametersA.count)"
      }
         speakB.append("\(((graphicsT ? 4 : 2) & Int(collectionh)))")
       var createK: String! = String(cString: [115,101,103,119,105,116,0], encoding: .utf8)!
       var menu9: String! = String(cString: [99,97,110,111,110,105,99,97,108,105,122,101,100,0], encoding: .utf8)!
       var playT: String! = String(cString: [104,109,97,99,108,105,115,116,0], encoding: .utf8)!
       var recognizeds: String! = String(cString: [100,116,115,109,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         parametersA = [((graphicsT ? 5 : 5) + ossR.count)]
      }
      if (Int(collectionh) / (Swift.max(ossR.count, 1))) >= 5 {
         ossR = "\(Int(collectionh) << (Swift.min(labs(1), 2)))"
      }
      while (1.11 == (5.67 * collectionh) || 2 == (Int(collectionh) * json0.count)) {
          var speakA: Bool = true
          var tapj: String! = String(cString: [97,99,99,117,109,117,108,97,116,101,100,0], encoding: .utf8)!
         collectionh *= (Double(speakB == (String(cString:[90,0], encoding: .utf8)!) ? json0.count : speakB.count))
         speakA = (((speakA ? tapj.count : 16) & tapj.count) >= 16)
         break
      }
       var lastc: Int = 5
      failedm += Double(parametersA.count ^ commonu.values.count)
      if failedm == 2082942.0 {
         break
      }
   } while (failedm == 2082942.0) && (speedH)
    
    if let vipValue = UserDefaults.standard.string(forKey: "VIP"), vipValue == "1" {

       var commentZ: Int = 1
       var speakV: String! = String(cString: [115,112,101,97,107,105,110,103,0], encoding: .utf8)!
      for _ in 0 ..< 3 {
         commentZ >>= Swift.min(5, labs(3))
      }
         speakV = "\(commentZ >> (Swift.min(labs(1), 4)))"
      while (1 < (speakV.count / (Swift.max(8, commentZ)))) {
          var collp: String! = String(cString: [112,114,111,112,0], encoding: .utf8)!
          var systemc: String! = String(cString: [102,105,108,108,105,110,0], encoding: .utf8)!
          var popupt: Int = 3
         speakV.append("\(commentZ / 3)")
         collp = "\(popupt)"
         systemc = "\(systemc.count)"
         popupt |= (systemc == (String(cString:[73,0], encoding: .utf8)!) ? collp.count : systemc.count)
         break
      }
      for _ in 0 ..< 1 {
          var bottomc: String! = String(cString: [100,99,116,101,108,101,109,0], encoding: .utf8)!
          var homeI: [Any]! = [String(cString: [103,114,97,112,104,113,108,0], encoding: .utf8)!, String(cString: [97,114,116,105,99,108,101,115,0], encoding: .utf8)!]
          var collatem: String! = String(cString: [115,112,101,99,116,114,97,0], encoding: .utf8)!
          var sourceU: String! = String(cString: [111,97,101,112,0], encoding: .utf8)!
         commentZ /= Swift.max(commentZ, 1)
         bottomc = "\(sourceU.count)"
         homeI.append((bottomc == (String(cString:[89,0], encoding: .utf8)!) ? sourceU.count : bottomc.count))
         collatem = "\(bottomc.count)"
      }
          var speedN: Double = 5.0
          var not_k5: Int = 1
          var enter6: Int = 3
         commentZ /= Swift.max(5, enter6 - commentZ)
         speedN /= Swift.max(Double(Int(speedN) ^ 3), 5)
         not_k5 += 2 % (Swift.max(not_k5, 8))
         enter6 <<= Swift.min(1, labs(Int(speedN)))
      for _ in 0 ..< 2 {
         commentZ <<= Swift.min(labs(3), 5)
      }
      speedH = !speedH
        return true
        
    } else {
        
        if let free = UserDefaults.standard.object(forKey: "free") as? Int {
      
            if let count = UserDefaults.standard.object(forKey: "count") as? Int {
                if free > count {
                    return false
                }
                
                return true
            }
        }
        else {
            UserDefaults.standard.set(1, forKey: "free")
        }
    }
    
    
    return true
   while (!commonu.values.contains { $0 as? Double == failedm }) {
      failedm /= Swift.max(3, (Double(Int(failedm) & (speedH ? 4 : 2))))
      break
   }
      speedH = (38 < ((speedH ? 38 : commonu.count) / (Swift.max(3, commonu.count))))
}


func mineFreeNumber() {
       var savek: [Any]! = [893, 644, 720]
    var servicez: Float = 4.0
   if servicez < Float(savek.count) {
      savek.append(Int(servicez))
   }

    DXAlamofireMine.shared.normalPost(urlSuffix: "/app/getSum") { result in
      servicez /= Swift.max(Float(1 * savek.count), 3)
        
        switch result {
        case.success(let model):
            
            if let obj = model as? NSDictionary, let code = obj["code"] as? Int {
                if code == 200 {
                    
                    if let count = obj["data"] as? String {
                        UserDefaults.standard.set(Int(count), forKey: "count")
                    }
                    
                    if let goods = UserDefaults.standard.object(forKey: "goods") as? Int {
                        
                        if goods == 1 {
                            UserDefaults.standard.set(5, forKey: "count")
                        }
                        
                    }
                    
                }
                else
                {
                    UserDefaults.standard.set(2, forKey: "count")
                }
                
                NotificationCenter.default.post(name: NSNotification.Name("updateFreeCountNotificationName"), object: nil)
                
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "接口请求出错")
   while ((servicez - 3.13) >= 3.55) {
      servicez /= Swift.max(Float(savek.count + 1), 2)
      break
   }
            break
        }
    }
   for _ in 0 ..< 3 {
       var desclabelS: Float = 1.0
       var graphicsr: String! = String(cString: [115,112,101,108,108,0], encoding: .utf8)!
         desclabelS -= Float(Int(desclabelS) >> (Swift.min(graphicsr.count, 1)))
      if 4.74 <= desclabelS {
         desclabelS += Float(Int(desclabelS) * 2)
      }
       var ylabelA: String! = String(cString: [100,105,114,97,99,116,97,98,0], encoding: .utf8)!
      while (desclabelS > Float(ylabelA.count)) {
         desclabelS *= Float(graphicsr.count | Int(desclabelS))
         break
      }
          var playr: String! = String(cString: [98,103,112,104,99,104,101,99,107,0], encoding: .utf8)!
          var picked7: String! = String(cString: [101,110,97,98,108,101,100,0], encoding: .utf8)!
         desclabelS -= Float(picked7.count >> (Swift.min(labs(1), 3)))
         playr = "\(playr.count)"
         picked7 = "\(3 & playr.count)"
      repeat {
         desclabelS /= Swift.max((Float((String(cString:[95,0], encoding: .utf8)!) == graphicsr ? graphicsr.count : ylabelA.count)), 3)
         if 4183956.0 == desclabelS {
            break
         }
      } while (4183956.0 == desclabelS) && (graphicsr.hasSuffix("\(desclabelS)"))
      savek.append(1)
   }
}


func clearCache() {
       var alifasth: Double = 1.0
    var strp: String! = String(cString: [112,97,114,0], encoding: .utf8)!
    var speedt: Float = 5.0
      alifasth += Double(3 | Int(speedt))

   repeat {
      speedt -= Float(Int(alifasth) * strp.count)
      if speedt == 2465264.0 {
         break
      }
   } while (speedt == 2465264.0) && ((strp.count + 2) <= 2 && (speedt - 1.89) <= 3.17)
    if let appBundle = Bundle.main.bundleIdentifier {
        
        SVProgressHUD.show()
   while ((strp.count * Int(speedt)) == 5 && (Int(speedt) * 5) == 1) {
       var chooseb: String! = String(cString: [112,101,114,115,105,115,116,101,100,0], encoding: .utf8)!
       var respondery: [String: Any]! = [String(cString: [111,100,100,0], encoding: .utf8)!:594, String(cString: [112,114,105,111,114,0], encoding: .utf8)!:143]
      if chooseb.hasPrefix("\(respondery.values.count)") {
          var enableds: Bool = true
         respondery["\(chooseb)"] = 3
         enableds = !enableds
      }
      while (2 <= (chooseb.count >> (Swift.min(labs(5), 5)))) {
         chooseb = "\(chooseb.count / 1)"
         break
      }
      for _ in 0 ..< 2 {
         respondery = ["\(respondery.values.count)": 1 ^ respondery.keys.count]
      }
      repeat {
         respondery["\(chooseb)"] = 1
         if respondery.count == 3750293 {
            break
         }
      } while (respondery.count == 3750293) && (3 < (chooseb.count >> (Swift.min(4, respondery.keys.count))) || 3 < (respondery.keys.count >> (Swift.min(labs(3), 3))))
       var iconY: Bool = true
       var infoK: Bool = false
         respondery = [chooseb: 3]
      strp = "\(1 | Int(speedt))"
      break
   }
        
        var messages = 0
      alifasth /= Swift.max(Double(1 - Int(speedt)), 5)
        if let free = UserDefaults.standard.object(forKey: "free") as? Int {
            messages = free
        }
        
        UserDefaults.standard.removePersistentDomain(forName: appBundle)
      speedt /= Swift.max(4, Float(2))
        NotificationCenter.default.post(name: NSNotification.Name("loginFailNotificationName"), object: nil)
        NotificationCenter.default.post(name: NSNotification.Name("UpdateTableViewNotificationName"), object: nil)
        NotificationCenter.default.post(name: NSNotification.Name("UpdateBashouNotificationName"), object: nil)
        
        UserDefaults.standard.set(messages, forKey: "free")
        UserDefaults.standard.set(1, forKey: "first")
        checkAliToken()
    }
}
